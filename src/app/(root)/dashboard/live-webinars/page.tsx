import { LiveWebinars } from "@/components/dashboard/live-webinars-tab";
import React from "react";

const page = () => {
  return (
    <div>
      <LiveWebinars />
    </div>
  );
};

export default page;
